# Simple Navigation Bar

This is a small HTML and CSS example that creates a basic navigation bar at the top of the page.

## 🧠 Features
- A **horizontal navigation bar** with three links: Home, About, Contact.
- Hover effect changes link background and text color.
- Uses **internal CSS** in the `<style>` section.

## 💻 How to Run
1. Save the file as `navbar.html`.
2. Open it in a browser.
3. Move your mouse over the navigation links to see the hover effect.

## 🧩 Technologies Used
- HTML
- CSS (internal style)
